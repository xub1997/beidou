spring:
  application:
    name: logger
  cloud:
    config:
      uri: http://localhost:5000
      fail-fast: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    publisher-confirms: true
    virtual-host: /